# Figure icon assets

Consistent Lucide outline icons retrieved with `better-icons`. Icons retain the
original 24 x 24 viewBox and use `currentColor` so the R figure can apply one
publication palette at render time. Lucide icons are distributed under the ISC
License.
